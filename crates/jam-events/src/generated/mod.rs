//! Auto-generated event types. Per-field docs live in `events.toml`; we don't
//! duplicate them onto every Rust field. Per-event docs are still required and
//! emitted by codegen from each event's `description = "..."` field.

#![allow(missing_docs)]

pub use self::types::*;

mod types;

