// AUTO-GENERATED by tools/events-codegen.py from crates/jam-events/events.toml.
// DO NOT EDIT BY HAND. Re-run the codegen script to update.
//
// Per `dec-events-toml-manifest` (spec §4.4.3): events.toml is the single
// source of truth. CI verifies this file is in sync via `--check`.


#![allow(clippy::module_name_repetitions)]

use serde::{Deserialize, Serialize};

/// Manifest schema version (from events.toml).
pub const MANIFEST_SCHEMA_VERSION: u32 = 1;

/// Common shape implemented by every generated event payload.
///
/// Provides compile-time access to the event type identifier and
/// `event_subtype_version` so call sites cannot accidentally use the wrong
/// version when constructing an envelope.
pub trait Event: Serialize + serde::de::DeserializeOwned {
    /// Kebab-case dotted event type (e.g. `"picker.spawned"`).
    const EVENT_TYPE: &'static str;
    /// Per-event-type version, bumped on additive changes only.
    const EVENT_SUBTYPE_VERSION: u32;
}

/// trunk-fetcher recomputed per-worktree staleness.
///
/// Event type: `branch.staleness-updated` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BranchStalenessUpdated {
    pub task_id: String,
    pub commits_behind: u32,
    pub commits_ahead: u32,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for BranchStalenessUpdated {
    const EVENT_TYPE: &'static str = "branch.staleness-updated";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// trunk-fetcher observed origin/<trunk> moved (§4.4.6).
///
/// Event type: `branch.trunk-moved` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct BranchTrunkMoved {
    pub project: String,
    pub old_sha: String,
    pub new_sha: String,
    pub fetched_at: chrono::DateTime<chrono::Utc>,
}

impl Event for BranchTrunkMoved {
    const EVENT_TYPE: &'static str = "branch.trunk-moved";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// clock-watcher detected NTP drift > 1s.
///
/// Event type: `clock.unsynced` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct ClockUnsynced {
    pub drift_ms: u64,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for ClockUnsynced {
    const EVENT_TYPE: &'static str = "clock.unsynced";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Human accepted a skill evolution candidate via git commit.
///
/// Event type: `evolve.skill-promoted` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EvolveSkillPromoted {
    pub skill_path: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for EvolveSkillPromoted {
    const EVENT_TYPE: &'static str = "evolve.skill-promoted";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Human rejected a skill evolution candidate.
///
/// Event type: `evolve.skill-rejected` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EvolveSkillRejected {
    pub skill_path: String,
    pub candidate_path: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for EvolveSkillRejected {
    const EVENT_TYPE: &'static str = "evolve.skill-rejected";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// skill-suspicion-reconciler observed >=3 dead_end entries in 7d for a skill (§7.4).
///
/// Event type: `evolve.skill-under-suspicion` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct EvolveSkillUnderSuspicion {
    pub skill_scope: String,
    pub dead_end_count: u32,
    pub since_days: u32,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub implicating_traces: Option<String>,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for EvolveSkillUnderSuspicion {
    const EVENT_TYPE: &'static str = "evolve.skill-under-suspicion";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// harness-version-watcher detected drift between installed binary and lockfile.
///
/// Event type: `harness.version-changed` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct HarnessVersionChanged {
    pub harness: String,
    pub expected: String,
    pub installed: String,
    pub detected_at: chrono::DateTime<chrono::Utc>,
}

impl Event for HarnessVersionChanged {
    const EVENT_TYPE: &'static str = "harness.version-changed";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Daily budget 100% — auto-pause-dispatch, ntfy urgently.
///
/// Event type: `maestro.budget.daily-exceeded` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MaestroBudgetDailyExceeded {
    pub spent_usd: f64,
    pub budget_usd: f64,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for MaestroBudgetDailyExceeded {
    const EVENT_TYPE: &'static str = "maestro.budget.daily-exceeded";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Per-session budget hit 125% — hard-abort, dump partial state, ntfy human.
///
/// Event type: `maestro.budget.hard-exceeded` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MaestroBudgetHardExceeded {
    pub session_id: String,
    pub spent_usd: f64,
    pub budget_usd: f64,
    pub dump_path: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for MaestroBudgetHardExceeded {
    const EVENT_TYPE: &'static str = "maestro.budget.hard-exceeded";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Per-session budget hit 100% — Maestro finishes current turn, aborts next unless extended (§4.1.4).
///
/// Event type: `maestro.budget.soft-exceeded` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MaestroBudgetSoftExceeded {
    pub session_id: String,
    pub spent_usd: f64,
    pub budget_usd: f64,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for MaestroBudgetSoftExceeded {
    const EVENT_TYPE: &'static str = "maestro.budget.soft-exceeded";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Maestro session closed (done | budget-exceeded | interrupted | fatal).
///
/// Event type: `maestro.session-ended` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MaestroSessionEnded {
    pub session_id: String,
    pub outcome: String,
    pub duration_ms: u64,
    pub input_tokens: u64,
    pub output_tokens: u64,
    pub cost_usd: f64,
    pub tool_calls_count: u32,
    pub ended_at: chrono::DateTime<chrono::Utc>,
}

impl Event for MaestroSessionEnded {
    const EVENT_TYPE: &'static str = "maestro.session-ended";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Maestro wake handler opened a new session.
///
/// Event type: `maestro.session-started` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MaestroSessionStarted {
    pub session_id: String,
    pub wake_source: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub wake_subject: Option<String>,
    pub started_at: chrono::DateTime<chrono::Utc>,
}

impl Event for MaestroSessionStarted {
    const EVENT_TYPE: &'static str = "maestro.session-started";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Maestro invoked a tool service via NATS request-reply (every call is journaled).
///
/// Event type: `maestro.tool-call` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct MaestroToolCall {
    pub session_id: String,
    pub tool_name: String,
    pub duration_ms: u64,
    pub success: bool,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub error_kind: Option<String>,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for MaestroToolCall {
    const EVENT_TYPE: &'static str = "maestro.tool-call";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Atomic-swap complete — manifest now points at new version (§20.3).
///
/// Event type: `patch.applied` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PatchApplied {
    pub service: String,
    pub from_version: String,
    pub to_version: String,
    pub subject_prefix: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PatchApplied {
    const EVENT_TYPE: &'static str = "patch.applied";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Post-patch deterministic health checks all passed.
///
/// Event type: `patch.confirmed` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PatchConfirmed {
    pub service: String,
    pub version: String,
    pub checks_run: u32,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PatchConfirmed {
    const EVENT_TYPE: &'static str = "patch.confirmed";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Patch agent could not recover — incident dump written, ntfy critical.
///
/// Event type: `patch.failed` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PatchFailed {
    pub service: String,
    pub incident_id: String,
    pub summary: String,
    pub incident_dir: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PatchFailed {
    const EVENT_TYPE: &'static str = "patch.failed";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Patch agent acquired the NATS KV patch-lock (TTL 5min).
///
/// Event type: `patch.lock-acquired` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PatchLockAcquired {
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PatchLockAcquired {
    const EVENT_TYPE: &'static str = "patch.lock-acquired";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Patch agent released the lock.
///
/// Event type: `patch.lock-released` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PatchLockReleased {
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PatchLockReleased {
    const EVENT_TYPE: &'static str = "patch.lock-released";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Mechanical rollback — manifest reverted to previous version.
///
/// Event type: `patch.rolled-back` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PatchRolledBack {
    pub service: String,
    pub from_version: String,
    pub to_version: String,
    pub reason: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PatchRolledBack {
    const EVENT_TYPE: &'static str = "patch.rolled-back";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Rollback resolved the issue — health checks pass post-rollback.
///
/// Event type: `patch.rolled-back-successfully` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PatchRolledBackSuccessfully {
    pub service: String,
    pub version: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PatchRolledBackSuccessfully {
    const EVENT_TYPE: &'static str = "patch.rolled-back-successfully";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// New binary present in ~/.jam/staging/ — patch-agent will pick this up.
///
/// Event type: `patch.staged` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PatchStaged {
    pub service: String,
    pub version: String,
    pub staging_path: String,
    pub binary_sha256: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PatchStaged {
    const EVENT_TYPE: &'static str = "patch.staged";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Picker raised an internal error before exit — Maestro wakes on this.
///
/// Event type: `picker.errored` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PickerErrored {
    pub session_id: String,
    pub task_id: String,
    pub error_kind: String,
    pub detail: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PickerErrored {
    const EVENT_TYPE: &'static str = "picker.errored";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Picker process exited normally (zero or non-zero, but not killed).
///
/// Event type: `picker.exited` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PickerExited {
    pub session_id: String,
    pub task_id: String,
    pub exit_code: u32,
    pub exited_at: chrono::DateTime<chrono::Utc>,
    pub duration_ms: u64,
}

impl Event for PickerExited {
    const EVENT_TYPE: &'static str = "picker.exited";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// First non-empty stdout/stderr line from a Picker — used by task-lifecycle-handler to mark first-progress.
///
/// Event type: `picker.first-output` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PickerFirstOutput {
    pub session_id: String,
    pub task_id: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PickerFirstOutput {
    const EVENT_TYPE: &'static str = "picker.first-output";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Picker reached prompt-waiting state with no queued message — Maestro may decide to wake.
///
/// Event type: `picker.idle` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PickerIdle {
    pub session_id: String,
    pub task_id: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for PickerIdle {
    const EVENT_TYPE: &'static str = "picker.idle";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Picker terminated via full-stop (§5.7) — SIGTERM 2s grace then SIGKILL.
///
/// Event type: `picker.killed` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PickerKilled {
    pub session_id: String,
    pub task_id: String,
    pub reason: String,
    pub killed_at: chrono::DateTime<chrono::Utc>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub diff_snapshot: Option<String>,
    pub from: String,
}

impl Event for PickerKilled {
    const EVENT_TYPE: &'static str = "picker.killed";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Emitted by jam-svc-session after a Picker process is launched (§24.3 step 10).
///
/// Event type: `picker.spawned` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PickerSpawned {
    pub task_id: String,
    pub harness: String,
    pub session_id: String,
    pub worktree_path: String,
    pub spawned_at: chrono::DateTime<chrono::Utc>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub picker_pid: Option<u32>,
    pub picker_trace_id: jam_trace::TraceId,
    pub maestro_trace_id: jam_trace::TraceId,
    pub sandbox_backend: String,
    pub sandbox_profile: String,
    pub task_class: String,
}

impl Event for PickerSpawned {
    const EVENT_TYPE: &'static str = "picker.spawned";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// stall-detector escalation (§4.4.6): token-idle, tool-loop, or no-progress.
///
/// Event type: `picker.stalled` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PickerStalled {
    pub session_id: String,
    pub task_id: String,
    pub stall_kind: String,
    pub stall_secs: u32,
    pub detected_at: chrono::DateTime<chrono::Utc>,
}

impl Event for PickerStalled {
    const EVENT_TYPE: &'static str = "picker.stalled";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// CI status flipped on a PR (running -> success/failure/cancelled).
///
/// Event type: `pr.ci.status-changed` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PrCiStatusChanged {
    pub pr_ref: String,
    pub task_id: String,
    pub ci_status: String,
    pub changed_at: chrono::DateTime<chrono::Utc>,
}

impl Event for PrCiStatusChanged {
    const EVENT_TYPE: &'static str = "pr.ci.status-changed";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// PR merged via GitHub UI (the only merge path — see principle-no-auto-merge).
///
/// Event type: `pr.merged` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PrMerged {
    pub pr_ref: String,
    pub task_id: String,
    pub merged_sha: String,
    pub merged_by: String,
    pub merged_at: chrono::DateTime<chrono::Utc>,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub touched_paths: Option<String>,
}

impl Event for PrMerged {
    const EVENT_TYPE: &'static str = "pr.merged";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Picker opened a PR via `open-pr` tool or `gh pr create` from the worktree.
///
/// Event type: `pr.opened` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PrOpened {
    pub task_id: String,
    pub pr_ref: String,
    pub branch: String,
    pub title: String,
    pub draft: bool,
    pub opened_at: chrono::DateTime<chrono::Utc>,
}

impl Event for PrOpened {
    const EVENT_TYPE: &'static str = "pr.opened";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// pr-status-poller observed a new review comment (CodeRabbit, codex-review, human reviewer).
///
/// Event type: `pr.review-received` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct PrReviewReceived {
    pub pr_ref: String,
    pub task_id: String,
    pub reviewer: String,
    pub artifact_count: u32,
    pub received_at: chrono::DateTime<chrono::Utc>,
}

impl Event for PrReviewReceived {
    const EVENT_TYPE: &'static str = "pr.review-received";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// A harness's window is exhausted (Codex 5h window full, Claude rate-limit hit, API budget cap).
///
/// Event type: `quota.exhausted` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct QuotaExhausted {
    pub harness: String,
    pub window_kind: String,
    #[serde(skip_serializing_if = "Option::is_none")]
    pub resets_at: Option<chrono::DateTime<chrono::Utc>>,
    pub detected_at: chrono::DateTime<chrono::Utc>,
}

impl Event for QuotaExhausted {
    const EVENT_TYPE: &'static str = "quota.exhausted";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Predictive: within 10% of exhaustion — Maestro can pre-emptively reroute.
///
/// Event type: `quota.exhausted-soon` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct QuotaExhaustedSoon {
    pub harness: String,
    pub window_kind: String,
    pub remaining: f64,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for QuotaExhaustedSoon {
    const EVENT_TYPE: &'static str = "quota.exhausted-soon";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// 5h window reset / API budget topped up / rate-limit cleared.
///
/// Event type: `quota.refilled` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct QuotaRefilled {
    pub harness: String,
    pub window_kind: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for QuotaRefilled {
    const EVENT_TYPE: &'static str = "quota.refilled";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// jam setup completed successfully — written to NATS KV setup-result bucket too (§11.4).
///
/// Event type: `setup.completed` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SetupCompleted {
    pub checks_passed: u32,
    pub checks_total: u32,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for SetupCompleted {
    const EVENT_TYPE: &'static str = "setup.completed";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Task explicitly abandoned (purge-session called or all retries exhausted).
///
/// Event type: `task.abandoned` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TaskAbandoned {
    pub task_id: String,
    pub reason: String,
    pub abandoned_at: chrono::DateTime<chrono::Utc>,
}

impl Event for TaskAbandoned {
    const EVENT_TYPE: &'static str = "task.abandoned";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Emitted by jam CLI when caleb runs `jam task spawn` — opens the root trace.
///
/// Event type: `task.requested` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TaskRequested {
    pub task_id: String,
    pub description: String,
    pub project: String,
    pub task_class: String,
    pub priority: String,
    pub requested_by: String,
}

impl Event for TaskRequested {
    const EVENT_TYPE: &'static str = "task.requested";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// tempyr journal flush published one or more sessions to refs/tempyr/journals/archive/*.
///
/// Event type: `tempyr.journal-flushed` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TempyrJournalFlushed {
    pub session_count: u32,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for TempyrJournalFlushed {
    const EVENT_TYPE: &'static str = "tempyr.journal-flushed";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Tempyr's file watcher detected a node edit on disk.
///
/// Event type: `tempyr.node-changed` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TempyrNodeChanged {
    pub node_id: String,
    pub path: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for TempyrNodeChanged {
    const EVENT_TYPE: &'static str = "tempyr.node-changed";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// tempyr-pr-reconciler or Maestro flagged a Tempyr node as possibly stale.
///
/// Event type: `tempyr.update-candidate` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TempyrUpdateCandidate {
    pub node_id: String,
    pub source: String,
    pub reason: String,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for TempyrUpdateCandidate {
    const EVENT_TYPE: &'static str = "tempyr.update-candidate";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}

/// Retry exhausted on a Tempyr write — needs human (§4.6.4).
///
/// Event type: `tempyr.write-permanently-failed` (version 1).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct TempyrWritePermanentlyFailed {
    pub node_id: String,
    pub operation: String,
    pub last_error: String,
    pub attempts: u32,
    pub ts: chrono::DateTime<chrono::Utc>,
}

impl Event for TempyrWritePermanentlyFailed {
    const EVENT_TYPE: &'static str = "tempyr.write-permanently-failed";
    const EVENT_SUBTYPE_VERSION: u32 = 1;
}
